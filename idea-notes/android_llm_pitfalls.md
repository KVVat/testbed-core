# LLMがAndroid UI操作で混乱するポイント分析

実際のデバイス (Pixel 8 / Android 17) とMCPツールの挙動を調査し、LLMエージェントが混乱・失敗しやすいポイントをまとめました。

---

## 1. UIダンプの構造的な問題

### 1.1 巨大なJSONレスポンス（トークン爆発）

実際に取得したダンプは **約42KB、1行** の巨大JSONです。一般的な画面でノード数は50〜200に達し、LLMのコンテキストウィンドウを圧迫します。

> [!WARNING]
> **影響**: LLMがJSON全体を解析しきれず、座標やresource IDの読み取りを間違える。特に画面下部のノードが切り捨てられやすい。

### 1.2 同一 `resourceId` の重複

今回のダンプでは、チェックボックスがすべて `android:id/checkbox` で、タイトルがすべて `android:id/title` です。

```
"Runtime Permissions"  → android:id/title  bounds [68,456]-[466,513]
"Signature"            → android:id/title  bounds [68,597]-[246,654]
"Pie(9)"               → android:id/title  bounds [68,738]-[180,795]
...
```

> [!CAUTION]
> **影響**: LLMが「`android:id/title` をクリック」と判断しても、**どのtitleか**が特定できない。テキスト内容＋座標の組み合わせで識別する必要があるが、LLMはresource IDだけで判断しがち。

### 1.3 `clickable` フラグの非直感的な配置

```
LinearLayout (clickable=true, bounds=[26,414]-[1054,555])
  └─ RelativeLayout (clickable=false)
      └─ TextView "Runtime Permissions" (clickable=false)
  └─ widget_frame
      └─ CheckBox (clickable=false, checkable=true)
```

> [!IMPORTANT]
> **影響**: テキスト「Runtime Permissions」のノード自体は `clickable=false`。タップすべきは**親のLinearLayout**。LLMはテキストノードの座標中心をタップしがちだが、それが有効でないケースがある（テキストの bounds がクリック領域外の場合）。CheckBoxも `clickable=false` で、実際は親のLinearLayoutが受け取る。

### 1.4 `naf` (Not Accessibility Friendly) フラグの蔓延

今回のダンプのほぼ全ノードが `naf: true` です。これはアプリがAccessibility対応を適切にしていないことを示しますが、LLMにとっては「このノードは操作可能なのか？」の判断材料になりません。

---

## 2. ダイアログ・オーバーレイの問題

### 2.1 ダイアログ表示時のツリー縮小

> [!WARNING]
> **問題**: Permission Dialogやシステムダイアログが表示されると、`rootInActiveWindow` が**ダイアログのウィンドウのみ**を返す場合がある。背景のアプリのノードが完全に消失する。

**LLMが混乱するパターン**:
1. アプリ画面を操作中 → パーミッションダイアログが表示
2. UIダンプを取得 → ダイアログ部分のノードのみ（数ノード）
3. LLMが「画面が変わった？アプリがクラッシュした？」と誤解
4. 戻るボタンを押してダイアログを却下してしまう

### 2.2 複数ウィンドウの問題

Android 12以降、`UiAutomation` はマルチウィンドウ対応 (`getRootInActiveWindow()` vs `getWindows()`) が可能ですが、現在のAgent実装は `rootInActiveWindow` のみ。

- **状態バー**、**ナビゲーションバー**、**通知ドロワー**は別ウィンドウ
- ダイアログも別ウィンドウの場合がある
- **分割画面モード**では2つのアプリウィンドウが存在

---

## 3. 座標計算の罠

### 3.1 ステータスバー・ナビゲーションバーのオフセット

画面解像度は `1080x2400` だが、実際のアプリ表示領域は上部のステータスバー（約132px）と下部のナビゲーションバー（約63px）で削られている。

```
ステータスバー: 0〜132px (約)
アプリ領域:     132〜2337px
ナビゲーション: 2337〜2400px
```

> [!NOTE]
> LLMが「画面の真ん中あたり」と考えて `(540, 1200)` をタップしても、実際のアプリ上の位置は想定と異なる場合がある。

### 3.2 小さいタップターゲット

CheckBoxの bounds: `[928,442]-[1012,526]` → **84x84 px**

LLMが座標計算を少し間違えるだけで、チェックボックスを外す可能性がある。特に：
- bounds の中心座標を正確に計算する必要がある
- テキストのboundsとチェックボックスのboundsが離れている

### 3.3 スクロール後の座標変化

`RecyclerView` (`scrollable: true`) をスクロールすると：
1. 全ノードの bounds が変化する
2. 画面外のノードはダンプに含まれない
3. LLMが「前回のダンプの座標」でタップすると間違った場所を押す

> [!CAUTION]
> **対策不足**: 現在の `swipe` ツールは座標指定だが、スクロール後にUIダンプを再取得する必要がある。このフローをLLMが理解できていないケースが多い。

---

## 4. タイミング/同期の問題

### 4.1 アニメーション中のダンプ取得

画面遷移アニメーション（300ms程度）中にダンプを取得すると：
- 遷移元と遷移先の混在した不完全なツリーが返る
- ノードが一時的に `enabled=false` になっている

現在の実装は `tap`/`swipe` 後にアイドル待ちしているが、すべてのアニメーション完了を保証するものではない。

### 4.2 非同期ダイアログの遅延出現

一部のパーミッション要求は、操作の**数百ms後**に表示される：
1. LLMがボタンをタップ
2. タップ結果のUIダンプは「元の画面」を返す（ダイアログはまだ表示されていない）
3. 次の操作を試みるが、その時にはダイアログが出ている
4. 意図しない場所をタップしてしまう

---

## 5. テキスト入力の問題

### 5.1 日本語・特殊文字

`input_text` は内部で `adb shell input text` を使うが：
- 日本語は入力不可（ASCII文字のみ）
- スペースは `%s` にエスケープが必要
- 記号（`@`, `#`, `$` 等）によっては予期しない入力になる

### 5.2 フォーカス状態の不確実性

テキスト入力前にフィールドがフォーカスされている必要があるが：
- LLMが `tap` でフィールドをタップ → ソフトキーボードが出現
- キーボード表示で画面レイアウトが変わる
- 元のUIダンプの座標が全て無効になる

---

## 6. MCP固有の実装上の問題

### 6.1 `press_key` のパラメータ不明確

現在のkeycodeは文字列指定（`"BACK"`, `"HOME"`）だが、agentが受け付けるkeycode形式が不明確。以下の疑問がLLMに生じる：
- `"KEYCODE_HOME"` なのか `"HOME"` なのか `"3"` なのか？
- 数値keycode（`3`, `4`, `66`）をそのまま文字列で渡せるのか？

### 6.2 エラー時の情報不足

`press_key` が `"Error: Failed to press key"` を返す場合、**何が失敗したのか**がわからない。タイムアウト？キーコード不正？Agent未起動？

### 6.3 `tap` の戻り値が常にUIダンプ

タップ失敗（座標外、画面外）でもUIダンプが返るため、LLMは「タップが成功した」と誤解しやすい。画面が変わらなかったことを前回のダンプと**差分比較**しないと成功/失敗が判定できない。

---

## 改善提案のサマリ

| 問題 | 優先度 | 改善案 |
|------|--------|--------|
| UIダンプの巨大さ | **高** | ダンプを階層的にサマライズする中間レイヤー（テキストノードのみ抽出等）を追加 |
| ダイアログでツリー縮小 | **高** | `getWindows()` でマルチウィンドウ対応、またはダイアログ検出フラグを追加 |
| 同一resourceId問題 | 中 | ノードにユニークなpath/indexを付与してLLMが参照できるようにする |
| エラー情報の不足 | 中 | 失敗時に具体的なエラー理由を返す |
| スクロール後の座標無効化 | 中 | `scroll_to_text` のような高レベルAPIをAgentに追加 |
| テキスト入力の制限 | 低 | ADB broadcast経由やClipboard経由の入力に対応 |
